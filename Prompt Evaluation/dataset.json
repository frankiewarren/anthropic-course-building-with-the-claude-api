[
  {
    "task": "Create a regular expression to validate an AWS IAM username, which must start with a letter, can contain letters, numbers, and these special characters (+=-,.@_), and be between 1-64 characters long",
    "format": "regex",
    "solution_criteria": "Regex must fully match the entire username, handle length constraints, and only allow specified characters"
  },
  {
    "task": "Write a Python function that converts an AWS CloudFormation resource type (e.g., 'AWS::EC2::Instance') into a compact, lowercase resource identifier",
    "format": "python",
    "solution_criteria": "Function should remove '::' separators, convert to lowercase, handle varied input lengths"
  },
  {
    "task": "Create a JSON object representing a basic AWS Lambda function configuration with minimal required properties",
    "format": "json",
    "solution_criteria": "JSON must include function name, runtime, handler, and role ARN with valid structure"
  }
]